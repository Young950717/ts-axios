import { describe, expect, test } from '@jest/globals'
import {
  buildURL,
  isURLSameOrigin,
  isAbsoluteURL,
  combineURL
} from '../../src/helpers/url'

describe('helper:url', () => {
  describe('buildURL', () => {

  })
})
